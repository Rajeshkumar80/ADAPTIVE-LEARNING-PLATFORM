# Core
fastapi
uvicorn[standard]
python-multipart
python-dotenv

# Database (SQLite is built-in to Python — no setup needed)
sqlalchemy

# Auth
python-jose[cryptography]
bcrypt

# Validation
pydantic
pydantic-settings
email-validator

# HTTP
httpx

# Optional AI (only loaded if API key is set)
# openai
